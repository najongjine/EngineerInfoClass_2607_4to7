#include <stdio.h>
#include <stdlib.h>

int main() {
    /* 배열의 장점 : 쉬운편
    배열의 단점: 한번 크기가 정해지면 못바꿈 */
    int a[3] = { 1,2,3 };
    int a2[] = { 1,2,3 };

    int* par;
    par = (int*)malloc(sizeof(int));
    *par = 5;
    
    par = (int*)realloc(par, sizeof(int) * 3);
    printf("par:%d \n", *par);


    return 0;
}
