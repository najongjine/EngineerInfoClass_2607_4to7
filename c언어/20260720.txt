
#include <stdio.h>

int main() {
	/* 배열과 포인터는 비슷해요
	배열하면 무조건 heap 영역에 공간 할당하고,
	거기에 시작주소를 ar1에 담는거에요 */
	int ar1[3] = {1,4,6};
	// 주소가 튀어 나와요. 왜냐면 ar1은 시작주소를 담고 있으니까요
	printf("ar1: %d \n", ar1); 

	// ar1이 저장하고 있는 주소 + 0을 한다음 거기 원산지 찾아가기
	printf("ar1[2]: %d \n", ar1[2]);

	for (int i = 0; i < 3;i++) {
		printf("ar1[%d]=%d \n", i, ar1[i]);
	}

	return 0;
}