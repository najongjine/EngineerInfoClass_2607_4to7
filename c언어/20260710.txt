//변수, 자료형

#include <stdio.h>


void main() {
	/* 주석 */
	/* 변수 a 
	* 변수를 선언할때 데이터 타입을 정해줘야해요
	* 모든 데이터는 메모리에 올려야 해요
	* 하지만 c,java는 멍청해서 메모리 몇칸 잡아야 할지 몰라요
	* 사람이 1칸잡아라, 4칸잡아라 이렇게 일일히 알려줘야해요
	int := 정수 */
	int a;
	a = 1;
	int a2 = a;
	a = a + a;

	/* c언어의 기본 자료형 */
	long n3; // 정수
	int n1; // 정수. 1.1 저장해도 상남자같이 1로 변함
	short n2; // 정수

	float f1; // 실수. 1을 저장해도 상남같이 1.0000 으로 표기
	double f2; // 실수
	
	char c1 // 문자. 'a' 이런거 "abcd 메롱" 이런거 아님

	
}